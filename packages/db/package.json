{
  "name": "@cgao/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "generate": "echo 'drizzle-kit generate is currently disabled (NodeNext .js resolution incompatibility in drizzle-kit 0.30). Edit packages/db/migrations/0000_initial.sql by hand and mirror changes in src/schema/*.ts.' && exit 0",
    "migrate": "psql \"$DATABASE_URL\" -f migrations/0000_initial.sql"
  },
  "dependencies": {
    "@cgao/schemas": "workspace:*",
    "drizzle-orm": "^0.36.0",
    "postgres": "^3.4.5"
  }
}
