{
  "name": "@cgao/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "generate": "echo 'TODO drizzle-kit generate' && exit 0",
    "migrate": "echo 'TODO drizzle-kit migrate' && exit 0"
  },
  "dependencies": {
    "@cgao/schemas": "workspace:*",
    "drizzle-orm": "^0.36.0"
  }
}
