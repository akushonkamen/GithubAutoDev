# T-M0-001..005 对账报告

**审计日期：** 2026-07-03
**审计触发：** commit `b01cdf0` 声称 T-M0-001..005 完成，但 tasklist checkbox 仍未勾选
**审计方式：** 对照 `docs/cgao_tasklist_v3.md` 各任务「产物」与「验收」逐项核对仓库实际状态

## 摘要

| 任务 | 声称状态 | 实际状态 | checkbox 决议 |
|---|---|---|---|
| T-M0-001 初始化 monorepo | 完成 | **不达标（大缺口）** | 保持 `[ ]` |
| T-M0-002 统一代码规范 | 完成 | **部分达标** | 保持 `[ ]` |
| T-M0-003 威胁模型文档 | 完成 | **部分达标** | 保持 `[ ]` |
| T-M0-004 安全回归测试框架 | 完成 | **未达标（完全未做）** | 保持 `[ ]` |
| T-M0-005 事件与错误码规范 | 完成 | **基本达标** | 勾选 `[x]` |

**结论：** commit 信息过度声明完成度。仅 T-M0-005 可视为达标；T-M0-001/004 严重缺产物；T-M0-002/003 缺部分子产物。M0 实际**仍在进行中**。

## 详细发现

### T-M0-001 初始化 monorepo — 不达标

**预期产物（tasklist 第 113–122 行）：**

```
apps/orchestrator
apps/runner-broker
packages/events
packages/policy
packages/github
packages/artifacts
packages/audit
infra/docker-compose.yml
```

**实际：**

| 产物 | 状态 |
|---|---|
| `apps/orchestrator` | ✅ 存在（typecheck/test 通过） |
| `apps/runner-broker` | ❌ **缺失** |
| `packages/events` | ✅ 存在（envelope + tests） |
| `packages/policy` | ❌ **缺失** |
| `packages/github` | ❌ **缺失** |
| `packages/artifacts` | ❌ **缺失** |
| `packages/audit` | ❌ **缺失** |
| `infra/docker-compose.yml` | ❌ **缺失**（无 `infra/` 目录） |

**额外存在但未在 tasklist 列出：** `packages/db`、`packages/schemas`、`packages/test-utils`。

**验收（tasklist 第 126–129 行）：**

> 本地可以启动 orchestrator、db、event bus、artifact mock
> CI 可运行 lint/typecheck/unit

- ❌ 无 `infra/docker-compose.yml` → 无法本地启动 db / event bus / artifact mock
- ❌ 无 `.github/workflows/` → CI 不存在
- ✅ 本地可跑 lint（修了下文 lint 失败后）/ typecheck / test

### T-M0-002 统一代码规范 — 部分达标

| 产物 | 状态 |
|---|---|
| eslint / prettier / tsconfig | ⚠️ 用 Biome 替代 eslint+prettier（合理替代）；`tsconfig.base.json` 严格模式 ✅ |
| commitlint | ❌ **缺失** —— `CONTRIBUTING.md` 描述了 `T-Mx-xxx <scope>: <summary>` 格式但无 commitlint 配置强制 |
| 测试目录规范 | ⚠️ 隐式（`__tests__/` 目录 + vitest），无显式规范文档 |
| 错误码规范 | ✅ `docs/standards/errors.md`（82 行，命名空间 + 通用码 + 语义） |
| 日志字段规范 | ❌ **缺失** —— 无 `docs/standards/logging.md` 或等价文档 |

**验收：**

> CI 中 lint/typecheck/test 全部通过

- ✅ typecheck 通过（5 个 workspace）
- ✅ test 通过（events 3、test-utils 3，其余 `--passWithNoTests`）
- ✅ lint 通过（本次审计修复 `.omc/` `.claude/` 排除规则后；详见下文「审计期修复」）

> 新增模块必须有基础单测

- ❌ `packages/db`、`packages/schemas` 无测试（仅 `--passWithNoTests`），新增模块未强制单测

### T-M0-003 威胁模型文档 — 部分达标

| 产物 | 状态 |
|---|---|
| `docs/threat-model.md` | ⚠️ 实际路径 `docs/security/threat-model.md`（合理位置），101 行，覆盖资产/信任边界/攻击面/控制项 |
| `attack-scenarios/prompt-injection.md` | ❌ **缺失** |
| `attack-scenarios/runner-exfiltration.md` | ❌ **缺失** |
| `attack-scenarios/stale-merge.md` | ❌ **缺失** |
| `attack-scenarios/webhook-replay.md` | ❌ **缺失** |

**验收覆盖检查：**

威胁模型正文中的攻击场景表（AS-01..AS-10、AS-IM-01..AS-IM-10）覆盖了以下类目：

| 验收要求覆盖 | threat-model.md 中映射 | 是否明确 |
|---|---|---|
| prompt injection | AS-03、AS-04、AS-IM-01 | ✅ |
| runner secret exfiltration | AS-06、AS-IM-05 | ✅ |
| forged marker | AS-05（LLM 越权）+ AS-IM-02（display_name 伪装） | ⚠️ 隐式，无独立「forged marker」编号 |
| stale approval | AS-07（SHA drift）+ §4.5 SHA-bound gates | ⚠️ 隐式 |
| TOCTOU merge | AS-07 + §4.5 SHA-bound gates | ⚠️ 隐式 |
| external PR artifact 污染 | spec §6.3 提及，threat-model.md **未独立编号** | ❌ |

**验收要求：每个攻击场景都有对应控制点和测试任务。**

- ✅ 控制点映射：C-IM-01..C-IM-14 + §4.1 baseline
- ❌ 测试任务映射：`tests/security/` 不存在（见 T-M0-004）

### T-M0-004 安全回归测试框架 — 完全未做

| 产物 | 状态 |
|---|---|
| `tests/security/` | ❌ **缺失** |
| `tests/fixtures/malicious-issues/` | ❌ **缺失** |
| `tests/fixtures/malicious-repos/` | ❌ **缺失** |
| `tests/fixtures/webhook-replay/` | ❌ **缺失** |

**验收：** 全部未满足。无 CI、无安全测试、无 merge 阻断机制。

**这是 M0 最大的实际缺口。**

### T-M0-005 事件与错误码规范 — 基本达标

| 产物 | 状态 |
|---|---|
| `packages/events/schemas/*.json` | ⚠️ 用 Zod schema（`packages/events/src/envelope.ts`）替代 JSON Schema —— 合理，运行时可校验且类型安全 |
| `packages/errors/codes.ts` | ❌ **缺失** —— 但 `docs/standards/errors.md` 第 82 行明确声明「`@cgao/errors` 包在 M1 落地。M0 仅在此文档登记命名空间与必备码」，按 spec 推迟到 M1 |
| `docs/event-contract.md` | ⚠️ 实际路径 `docs/standards/events.md`（59 行），等价文档 |

**验收：**

> 所有事件 schema 可校验

- ✅ `cgaoEventEnvelopeSchema` 通过 Zod 校验，`packages/events/src/__tests__/envelope.test.ts` 3 个测试通过

> 错误码包含 module、reason、retryable、severity

- ⚠️ `docs/standards/errors.md` 定义了 code/retryable/含义/cause/trace，**缺独立的 `severity` 字段**（用 retryable + 是否进入 DLQ 隐式表达）
- ⚠️ 「module」用 code 前缀（CFG/AUTH/...）隐式表达，无独立字段

**决议：** 基本达标，勾选 `[x]`，遗留 `severity` 显式字段问题至 M1 `@cgao/errors` 落地时解决。

## 审计期修复

本次审计过程中已修复的问题：

1. **lint 失败修复：** `biome.json` 第 10 行 `files.ignore` 增加 `.omc`、`.claude`，避免 OMC 状态文件被 Biome 格式化报错。
2. **tasklist checkbox 对齐：** T-M0-005 勾选 `[x]`；其余保持 `[ ]` 并加上 `[audit 2026-07-03]` 注释指向本报告。

## 后续行动（建议在 M0 收尾前完成）

按优先级排序，下列工作需在 M0 真正完结前完成：

### P0 — M0 收尾必做

1. **补 `apps/runner-broker` 骨架**（spec §13）—— 即便空实现，至少占位
2. **补 `packages/{policy,github,artifacts,audit}` 骨架** —— package.json + src/index.ts + 基础单测
3. **补 `infra/docker-compose.yml`** —— postgres + nats/redis + artifact mock 服务
4. **补 `.github/workflows/ci.yml`** —— lint/typecheck/test 全量跑
5. **补 `attack-scenarios/{prompt-injection,runner-exfiltration,stale-merge,webhook-replay}.md` 4 个文件**
6. **补 `tests/security/` 框架** —— 至少有一个 prompt injection fixture + webhook replay 测试可跑
7. **补 commitlint 配置** —— 强制 `T-Mx-xxx` 格式
8. **补 `docs/standards/logging.md`** —— 日志字段规范

### P1 — M0 收尾建议

9. 在 `docs/security/threat-model.md` 中显式编号 forged marker / stale approval / TOCTOU merge / external PR artifact 污染四个场景
10. 在错误码规范中显式加入 `severity` 字段
11. 给 `packages/db`、`packages/schemas` 各加一个 smoke 测试，移除 `--passWithNoTests` 依赖

### P2 — M1 起接入

12. 落地 `packages/errors/codes.ts`（tasklist 已推迟到 M1）

## 建议里程碑状态修订

`README.md` 第 51–57 行 M0 状态「进行中」**正确**，不应改。建议在 tasklist 第 105 行 `T-M0-001` 上方添加里程碑层级的 audit note，引用本报告。
